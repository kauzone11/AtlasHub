import { prisma } from "@/lib/prisma"; import { requireHubPermission } from "@/lib/hub/auth"; import { hubJson, withHubApi } from "@/lib/hub/api"; import { handleCreateCompetency } from "@/lib/hub/operations-handlers";
export const GET = withHubApi(async () => { const session = await requireHubPermission("people:access"); return hubJson({ competencies: await prisma.hubCompetency.findMany({ where: { organizationId: session.organizationId, isActive: true }, select: { id: true, name: true, description: true, category: true }, orderBy: { name: "asc" } }) }); });
export const POST = withHubApi(async (request) => { const session = await requireHubPermission("people:manage"); return hubJson(await handleCreateCompetency(prisma, session, await request.json().catch(() => null)), { status: 201 }); });
