import { prisma } from "@/lib/prisma"; import { requireHubPermission } from "@/lib/hub/auth"; import { hubJson, withHubApi } from "@/lib/hub/api"; import { handleUpdateDevelopmentGoal } from "@/lib/hub/operations-handlers";
export const PATCH = withHubApi<{ params: Promise<{ id: string }> }>(async (request, context) => { const session = await requireHubPermission("people:access"); return hubJson(await handleUpdateDevelopmentGoal(prisma, session, (await context.params).id, await request.json().catch(() => null))); });
