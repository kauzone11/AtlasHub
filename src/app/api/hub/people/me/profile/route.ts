import { prisma } from "@/lib/prisma"; import { requireHubPermission } from "@/lib/hub/auth"; import { hubJson, withHubApi } from "@/lib/hub/api"; import { handleUpdateOwnProfile } from "@/lib/hub/operations-handlers";
export const GET = withHubApi(async () => { const session = await requireHubPermission("people:access"); const profile = await prisma.hubMemberProfile.findFirst({ where: { memberId: session.memberId, organizationId: session.organizationId }, select: { phone: true, university: true, course: true, semester: true, linkedinUrl: true, bio: true, visibility: true, joinedAt: true } }); return hubJson({ profile }); });
export const PATCH = withHubApi(async (request) => { const session = await requireHubPermission("people:manage-own-profile"); return hubJson(await handleUpdateOwnProfile(prisma, session, await request.json().catch(() => null))); });
