import { prisma } from "@/lib/prisma"; import { requireHubPermission } from "@/lib/hub/auth"; import { hubJson, withHubApi } from "@/lib/hub/api"; import { handleCreateRecognition } from "@/lib/hub/operations-handlers";
export const GET = withHubApi(async () => { const session = await requireHubPermission("people:access"); return hubJson({ recognitions: await prisma.hubRecognition.findMany({ where: { organizationId: session.organizationId, OR: [{ visibility: "ORGANIZATION" }, { recipientMemberId: session.memberId }, { givenById: session.memberId }] }, select: { id: true, recipientMemberId: true, givenById: true, title: true, description: true, recognizedAt: true, visibility: true }, orderBy: { recognizedAt: "desc" }, take: 100 }) }); });
export const POST = withHubApi(async (request) => { const session = await requireHubPermission("people:access"); return hubJson(await handleCreateRecognition(prisma, session, await request.json().catch(() => null)), { status: 201 }); });
