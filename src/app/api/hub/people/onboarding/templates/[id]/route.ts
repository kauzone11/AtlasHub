import { prisma } from "@/lib/prisma"; import { requireHubPermission } from "@/lib/hub/auth"; import { hubJson, withHubApi } from "@/lib/hub/api"; import { handleUpdateOnboardingTemplate } from "@/lib/hub/operations-handlers";
export const PATCH = withHubApi(async (request, context: { params: Promise<{ id: string }> }) => { const session = await requireHubPermission("people:manage-directorate"); const { id } = await context.params; return hubJson(await handleUpdateOnboardingTemplate(prisma, session, id, await request.json().catch(() => null))); });
