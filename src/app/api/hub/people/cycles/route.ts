import { prisma } from "@/lib/prisma"; import { requireHubPermission } from "@/lib/hub/auth"; import { hubJson, withHubApi } from "@/lib/hub/api"; import { handleCreatePeopleCycle } from "@/lib/hub/operations-handlers";
export const GET = withHubApi(async () => { const session = await requireHubPermission("people:access"); return hubJson({ cycles: await prisma.hubPeopleCycle.findMany({ where: { organizationId: session.organizationId }, select: { id: true, name: true, status: true, startsAt: true, endsAt: true, updatedAt: true }, orderBy: { createdAt: "desc" } }) }); });
export const POST = withHubApi(async (request) => { const session = await requireHubPermission("people:cycles-manage"); return hubJson(await handleCreatePeopleCycle(prisma, session, await request.json().catch(() => null)), { status: 201 }); });
