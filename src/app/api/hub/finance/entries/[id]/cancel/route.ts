import { prisma } from "@/lib/prisma"; import { requireHubPermission } from "@/lib/hub/auth"; import { hubJson, withHubApi } from "@/lib/hub/api"; import { handleCancelFinancialEntry } from "@/lib/hub/operations-handlers";
export const POST = withHubApi<{ params: Promise<{ id: string }> }>(async (request, context) => { const session = await requireHubPermission("finance:create"); return hubJson(await handleCancelFinancialEntry(prisma, session, (await context.params).id, await request.json().catch(() => null))); });
