import { prisma } from "@/lib/prisma"; import { requireHubPermission } from "@/lib/hub/auth"; import { hubJson, withHubApi } from "@/lib/hub/api"; import { handleRecordSettlement } from "@/lib/hub/operations-handlers";
export const POST = withHubApi<{ params: Promise<{ id: string }> }>(async (request, context) => { const session = await requireHubPermission("finance:settle"); return hubJson(await handleRecordSettlement(prisma, session, (await context.params).id, await request.json().catch(() => null)), { status: 201 }); });
